namespace OperationDoctor.Placeholder
{
    // Placeholder-only state list for the first playable boundary wiring.
    public enum FirstPlayableFlowState
    {
        None = 0,
        QueueVisible = 1,
        ProfileVisible = 2,
        ActionSelected = 3,
        ResultVisible = 4,
        DecisionPending = 5,
        RewardVisible = 6,
    }
}
